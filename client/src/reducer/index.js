// import {
    
// } from "../constants";

const initialState = {
    shoes: [],
}

function rootReducer (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    } 
    
}

export default rootReducer;